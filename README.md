🎙️ تبدیل هوشمند صدا به متن فارسی (Persian Audio Transcriber - Ultra Edition)

این پروژه یک ابزار پیشرفته و چند-ریسمانی (Multi-threaded) با رابط کاربری جذاب برای تبدیل فایل‌های صوتی به متن فارسی است. نسخه جدید (Ultra) با قابلیت نمایش زنده وضعیت، سرعت پردازش و زمان باقی‌مانده، تجربه کاربری حرفه‌ای‌تری را ارائه می‌دهد.

✨ ویژگی‌های کلیدی

♾️ بدون محدودیت حجم و زمان: قابلیت پردازش فایل‌های بسیار حجیم و طولانی (حتی چند ساعته) بدون نگرانی از قطع شدن؛ برنامه به صورت خودکار فایل را مدیریت می‌کند.

🎵 پشتیبانی وسیع از فرمت‌ها: سازگاری با تمام فرمت‌های صوتی رایج شامل MP3, WAV, OGG, FLAC, M4A, AAC, WMA.

📊 مانیتورینگ زنده: دارای نوار پیشرفت (Progress Bar) که هر نیم ثانیه به‌روز می‌شود و درصد پیشرفت را نمایش می‌دهد.

⚡ محاسبه سرعت و زمان: نمایش سرعت پردازش (KB/s) و تخمین زمان باقی‌مانده (ETA) به صورت لحظه‌ای.

📂 پردازش گروهی: اسکن خودکار پوشه sot و تبدیل تمام فایل‌های صوتی موجود در صف به نوبت.

📝 خروجی استاندارد Word: تولید فایل‌های .docx با رعایت راست‌چین (RTL) و فونت مناسب.

🔄 مدیریت هوشمند خطا: سیستم تلاش مجدد (Retry) خودکار در صورت قطع شدن اینترنت.

🛠 اصلاح فونت فارسی: حل مشکل نمایش جدا جدا یا برعکس حروف فارسی در ترمینال ویندوز (CMD).

✂ بخش‌بندی خودکار: شکستن فایل‌های حجیم به قطعات کوچک برای سازگاری با API گوگل.

🚀 پیش‌نیازها (کامپیوتر)

برای اجرای صحیح برنامه روی ویندوز/مک/لینوکس تنها به مورد زیر نیاز دارید:

Python 3.x

(نکته: فایل‌های مورد نیاز FFmpeg در پروژه گنجانده شده‌اند و نیازی به نصب جداگانه ندارند.)

📦 نصب و راه‌اندازی (کامپیوتر)

۱. مخزن را کلون کنید یا فایل‌ها را دانلود نمایید.
۲. با دستور زیر، تمام کتابخانه‌های مورد نیاز را نصب کنید:

pip install SpeechRecognition pydub python-docx colorama arabic-reshaper python-bidi


🛠 نحوه استفاده (کامپیوتر)

روش اول: اجرای آسان (پیشنهادی)

۱. ابتدا فایل‌های صوتی خود را داخل پوشه sot کپی کنید (اگر این پوشه وجود ندارد، یک بار برنامه را اجرا کنید تا ساخته شود).
۲. روی فایل run.bat دوبار کلیک کنید تا عملیات تبدیل شروع شود.

روش دوم: از طریق ترمینال

۱. یک پوشه به نام sot در کنار فایل برنامه (transcribe.py) ایجاد کنید.
۲. فایل‌های صوتی خود را داخل پوشه sot کپی کنید.
۳. برنامه را اجرا کنید:

python transcribe.py


۴. پس از اتمام، فایل‌های متنی (Word) در پوشه outputs ذخیره می‌شوند.

⚙️ ساختار پروژه

Project/
│
├── transcribe.py    # موتور اصلی برنامه (نسخه Ultra)
├── run.bat          # فایل اجرای آسان (دوبار کلیک کنید)
├── ffmpeg.exe       # فایل اجرایی FFmpeg (موجود در پروژه)
├── ffprobe.exe      # فایل اجرایی FFprobe (موجود در پروژه)
├── sot/             # (ورودی) محل قرارگیری فایل‌های صوتی
├── outputs/         # (خروجی) محل ذخیره فایل‌های Word
└── README.md        # مستندات


❓ عیب‌یابی رایج

خطای FileNotFound یا WinError 2:

اگر فایل‌های ffmpeg.exe و ffprobe.exe را پاک کرده باشید این خطا رخ می‌دهد. مطمئن شوید این فایل‌ها در کنار برنامه اصلی هستند.

نمایش عجیب کلمات فارسی در CMD:

اگر کلمات جدا جدا هستند، یعنی کتابخانه‌های اصلاح متن نصب نشده‌اند. دستور pip install arabic-reshaper python-bidi را اجرا کنید.

ماندن روی 0% یا سرعت پایین:

این مورد معمولاً به سرعت اینترنت یا پاسخگویی سرورهای گوگل بستگی دارد. برنامه به طور خودکار سعی می‌کند اتصال را حفظ کند.

توسعه داده شده با ❤️
